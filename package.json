{
  "name": "swisse-verify-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check assets/app.js && node --check api/verify.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.4.0"
  }
}
